{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Debug Noaises",
            "type": "debugpy",
            "request": "launch",
            "module": "noaises.main",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            },
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}