# Optional — Claude API key for direct API access (enables background distillation)
# If not set, noaises uses Claude Code OAuth (interactive browser login)
ANTHROPIC_API_KEY=

# Optional — Azure Speech Services (for voice mode)
# Leave blank to run in text-only mode
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=westeurope
